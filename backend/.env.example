# Database
DB_URL=sqlite:///./scrum_mind.db

# Security / JWT
JWT_SECRET=CHANGE_ME
JWT_EXPIRES_MIN=60
JWT_ALGORITHM=HS256

# CORS
# Comma-separated list of origins. Use * for all (dev only).
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
